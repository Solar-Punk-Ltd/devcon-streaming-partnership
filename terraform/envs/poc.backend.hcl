# Committed on purpose: a bucket name is not a secret, and the copy step was a per-checkout
# manual step of exactly the kind M4 exists to eliminate. bootstrap/ creates this bucket.
bucket = "streaming-504704-devcon-tfstate"
prefix = "poc"
